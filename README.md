# Popsims

Calculates the population after an amount of generations.

Currently no builds released yet. Can run using the dotnet cli:

```sh
# from the project directory
dotnet run {INITIAL ENTITY COUNT} {GENERATIONS COUNT}
```
